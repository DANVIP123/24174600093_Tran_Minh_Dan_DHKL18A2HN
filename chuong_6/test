n = int(input("Nhập số nguyên dương n: "))
A = []
B = []

for i in range(1, n):
    if i % 2 == 1:
      A.append(i)
    else:
      B.append(i)

A.sort(reverse=True)
B.sort(reverse=True)

print("Danh sách số lẻ:", A)
print("Danh sách số chẵn:", B)